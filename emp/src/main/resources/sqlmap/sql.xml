<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dw.emp.mapper.EmpMapper">
	<select id="selectEmp" resultType="com.dw.emp.vo.EmpVO">
		SELECT e.empno, e.ename, e.job, e.hiredate, e.sal, d.dname, d.loc FROM emp AS e INNER JOIN dept AS d ON e.deptno = d.deptno
	</select>
	
</mapper>